# Test Images

This directory contains test images for CitySeg tests.

- `test_image.png`: A simple test image for basic processing tests
- `checkerboard.png`: A checkerboard pattern for predictable segmentation results

Images are relatively small to ensure tests run quickly.